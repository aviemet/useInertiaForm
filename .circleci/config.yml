version: 2.1

orbs:
  node: circleci/node@5.1.0

jobs:
  build:
    docker:
      # replace with your preferred image
      - image: circleci/node@5.1.0
    steps:
      - node/install-yarn

workflows:
    example-workflow:
      jobs:
        - node/test:
            pkg-manager: yarn 
        - build
